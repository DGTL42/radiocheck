# Default values. Override baseUrl with: mvn test -DbaseUrl="https://..."
base.url=https://your-test-environment.example.com/form

# Adapt these labels/selectors to your own form.
radio.label=Option A
submit.button.name.regex=confirm|submit|vote
success.message.regex=thank you|confirmed|success|submitted
already.submitted.message.regex=already voted|already submitted|duplicate|not allowed|one vote only
